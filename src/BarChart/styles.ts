import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ css }) => ({
  axis: css``,
  bar: css``,
  container: css`
    width: 100%;
    height: 100%;
  `,
  grid: css``,
  referenceLine: css``,
  root: css`
    font-size: 12px;
    min-height: 300px;
    width: 100%;
    height: 100%;
  `,
}));
